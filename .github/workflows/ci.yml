name: CI Pipeline

on:
  push:
    branches: [ "master" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Login Dockerhub
      env:
        davidocta154: ${{secrets.DOCKER_USERNAME}}
        davidocta123: ${{secrets.DOCKER_PASSWORD}}
      run: docker login -u $DOCKER_USERNAME -p $DOCKER_PASSWORD

    - name: Build docker image
      run: docker build -t dimasichiro/backend-magang .
    - name: Publish image to docker hub
      run: docker push dimasichiro/backend-magang:latest
